import { Meta } from "@storybook/addon-docs/blocks";
import breakpoints from "../assets/breakpoints.jpg";

<Meta title="Sass/Mixins/Breakpoints" />

# 🧪 Breakpoints

Mixins **globais** relacionados a **breakpoints**.

## 💈 Utilização

No arquivo em que a utilização dos **mixins** forem **necessárias**, importar:

``` scss
@import '~@ionic/core/templarios/scss/templarios';
```

Os breakpoints são quebras de layout padrões criadas com o intuito de padronizar as principais resoluções de viewport que as aplicações
do medgrupo necessitam.

<img style={{padding: "30px 0"}} src={breakpoints} alt="breakpoints" />

## 🧪 breakpoint-up()

### 💈 Utilização:

```
@include breakpoint-up('md') {
  .class {
    ...
  }
}
```

### Compila para:

```
@media (min-width: 768px) {
  .class { ... }
}
```

## 🧪 breakpoint-down()

### 💈 Utilização:

```
@include breakpoint-down('md') {
  .class {
    ...
  }
}
```

### Compila para:

```
@media (max-width: 991.98px) {
  .class { ... }
}
```

## 🧪 breakpoint-between()

### 💈 Utilização:

```
@include breakpoint-between('sm','lg') {
  .class {
    ...
  }
}
```

### Compila para:

```
@media (min-width: 576px) and (max-width: 991.98px) {
  .class { ... }
}
```

## 🧪 breakpoint-only()

### 💈 Utilização:

```
@include breakpoint-between('xxs') {
  .class {
    ...
  }
}
```

### Compila para:

```
@media (min-width: 340px) and (max-width: 379.98px) {
  .class { ... }
}
```
