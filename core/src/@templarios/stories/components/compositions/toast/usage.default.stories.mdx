import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Components/Compositions/Toast/Usage" />

# Toast



## Typescript

### Versao Sucesso (verde)

```
const createToastFloatBottom = async () => {
  toastController.create({
    header: 'Mensagem da Notificação.',
    position: 'bottom',
    showCloseButton: true,
    cssClass:"med-color-feedback med-color-fb-success",
    buttons: [
    {
      side: 'end',
      icon: 'med-fechar',
      role: 'cancel',
      handler: () => {
        console.log('Cancel clicked');
      }
    }
  ]
  }).then((toast) => {
    toast.present()
  })
}
```

### Versao Sucesso (vermelha)

```
const createToastFloatBottom = async () => {
  toastController.create({
    header: 'Mensagem da Notificação.',
    position: 'bottom',
    showCloseButton: true,
    cssClass:"med-color-feedback med-color-fb-warning",
    buttons: [
    {
      side: 'end',
      icon: 'med-fechar',
      role: 'cancel',
      handler: () => {
        console.log('Cancel clicked');
      }
    }
  ]
  }).then((toast) => {
    toast.present()
  })
}
```
