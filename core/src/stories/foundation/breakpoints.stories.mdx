import { Meta } from "@storybook/addon-docs/blocks";
import breakpoints from "../assets/breakpoints.jpg";

<Meta title="Foundation/Breakpoints" />

# Breakpoints

Os breakpoints são quebras de layout padrões criadas com o intuito de padronizar as principais resoluções de viewport que as aplicações
do medgrupo necessitam.

#### [[Link Figma]](https://www.figma.com/file/2j9jNt3PmQXpzD3IQJkyZe/Componentes?node-id=492%3A0)

#### É considerado desktop o brekpoint 'lg' ou 992px.

<img style={{padding: "30px 0"}} src={breakpoints} alt="breakpoints" />

# Map

Mapa contendo os breakpoints do DS.

```
$med-grid-breakpoints: (
  'xxs': 340px,
  'xs': 380px,
  'sm': 576px,
  'md': 768px,
  'lg': 992px,
  'xl': 1200px,
);
```

# Mixins

Utilizar os seguintes mixins para se referir a media queries.

## 🧪 breakpoint-up()

### 💈 Utilização:

```
@include breakpoint-up('md') {
  .class {
    ...
  }
}
```

### Compila para:

```
@media (min-width: 768px) {
  .class { ... }
}
```

## 🧪 breakpoint-down()

### 💈 Utilização:

```
@include breakpoint-down('md') {
  .class {
    ...
  }
}
```

### Compila para:

```
@media (max-width: 991.98px) {
  .class { ... }
}
```

## 🧪 breakpoint-between()

### 💈 Utilização:

```
@include breakpoint-between('sm','lg') {
  .class {
    ...
  }
}
```

### Compila para:

```
@media (min-width: 576px) and (max-width: 991.98px) {
  .class { ... }
}
```

## 🧪 breakpoint-only()

### 💈 Utilização:

```
@include breakpoint-between('xxs') {
  .class {
    ...
  }
}
```

### Compila para:

```
@media (min-width: 340px) and (max-width: 379.98px) {
  .class { ... }
}
```
